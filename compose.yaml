services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.local

    ports:
      - "3000:3000"

    volumes:
      - ./storage:/rails/storage

    environment:
      RAILS_ENV: development